{
	"blocks - decorators - registration - fails with multiple and args - 00": null,
	"blocks - decorators - should fail when accessing variables from root - 00": {
		"decorators": {
			"decorator": {
				"javascript": "function (fn, props, container, options) {\n          equals(options.args[0], undefined);\n          global.run = true;\n          return fn;\n        }"
			}
		}
	}
}
